# PDF to CSV - Generic Content Extractor
# Minimal dependencies for pure content extraction

# Core extraction
pdfplumber>=0.10.0          # PDF text and table extraction

# OCR support (optional - for scanned PDFs)
pytesseract>=0.3.10         # Tesseract OCR wrapper
Pillow>=10.0.0              # Image processing for OCR

# Web UI
streamlit>=1.29.0           # Web interface
pandas>=2.0.0               # Data handling for UI
